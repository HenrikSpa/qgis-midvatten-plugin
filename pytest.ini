[pytest]
testpaths = test
markers =
    spatialite: tests requiring a SpatialLite backend
    postgis: tests requiring a PostGIS backend
    active: generic active tests (no specific backend)
    xdist_group: group tests onto a single xdist worker (used for PostGIS isolation)
